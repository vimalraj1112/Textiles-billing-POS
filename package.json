{
  "name": "mathi-collections",
  "version": "1.0.0",
  "description": "Mathi Collections - Dress Shop POS (client + server)",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "start": "node server/src/server.js",
    "build": "npm run build --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "test": "npm test --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "seed": "npm run seed --prefix server"
  }
}